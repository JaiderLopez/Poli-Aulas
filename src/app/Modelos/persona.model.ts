export class Persona {
    id: string;
    name: string;
    email: string;
    password: string;
    type: string;
    state: string;
    subject: [][]; //docentes
    group: [][]; //docentes y estudiantes
}
