export class Persona {
    id: string;
    name: string;
    email: string;
    password: string;
    type: string;
    state: string;
    subject: string[]; //docentes
    group: string[]; //docentes y estudiantes
}
